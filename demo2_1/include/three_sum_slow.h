#ifndef __THREE_SUM_SLOW_H
#define __THREE_SUM_SLOW_H

#include "three_sum.h"

class ThreeSumSlow : public ThreeSum {
    public:
        int count(const int* nums);
        int num();
};

int ThreeSumSlow::count(const int* nums) {
    return -1;
}

int ThreeSumSlow::num() {
    return 0;
}

#endif
